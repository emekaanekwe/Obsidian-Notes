A type of data structure that is a mutable doubly-linked-list generalization of queues and stacks that allows for more efficient append and pop operations.

```Python
from collections import deque

# create empty deque
deque() 

```

### Purpose
1. useful if you want to keep the properties of a set, but need to .pop() or .append()