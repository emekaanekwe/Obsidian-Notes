# Terminology

  

## Perplexity

### Motivation

any good model assigns high probability to the natural text in the data. but as context grows, it gets exponentially smaller

	Using raw probabilities creates exponentially smaller units, so comparing sequences of different lengths is impossible

	Need a way to use N-th token analysis, i.e., the geometric mean of probability per token
### Informal

*measures how many equally likely choices he model thinks it has at each step*

**high** - model is choosing words more often from a uniform distribution
**low** - smaller distro. model is more confident

### Formal
*it is the inverse of the chain rule probability:*
$$P(x_1, ... ,x_N)=\prod^N_{n=1}P(x_n|x_1,...,x_n-1)^{-\frac{1}{N}}$$

# DS Requirements

## Data Understanding (DU) – 15 marks total

### DU1 – Data Understanding (5 marks)
- [ ] Explore the structure and statistical properties of the training corpus (`train.indexed.csv`).
- [ ] Produce **at most three plots** (e.g., token frequency histogram, document length histogram, document counts per category bar chart).
- [ ] State **at least two observations** grounded in your plots. Observations must go beyond simple description (e.g., discuss sparsity, smoothing needs, class imbalance, document length bias, etc.).

### DU2 – Stopword Identification (5 marks)
- [ ] Compute **Term Frequency (TF)** and **Document Frequency (DF)** using the training data only.
- [ ] Identify **40 stopword token IDs**.
- [ ] Report the **TF threshold** you used and justify your choice.
- [ ] Report the **DF (or IDF) threshold** you used and justify your choice.
### DU3 – Collocation Discovery (5 marks)
- [ ] Identify **at least 10 collocation pairs** (token ID pairs) from the training corpus using a statistical method.
- [ ] Provide a **clear explanation** of the method and strategy used.

---

## Part A – N‑gram Language Modeling & Catchphrase Insertion – 65 marks total

### A1 – N‑gram Language Modeling (45 marks)

#### General requirements
- [ ] Train **four n‑gram models** per category (20 categories) using **only** `train.indexed.csv`:
  - Maximum Likelihood Estimation (MLE) – implement from scratch.
  - Add‑1 smoothing – implement from scratch without using `nltk.lm.KneserNeyInterpolated`.
  - Add‑k smoothing – implement from scratch without using `nltk.lm.KneserNeyInterpolated`.
  - Kneser‑Ney (KN) – you **may** use `nltk.lm.KneserNeyInterpolated`.
- [ ] Choose a single n‑gram order `n ∈ {1,2,3}` and use it consistently across all four models. 
- [ ] Discuss the effect of n on perplexity in your notebook.
- [ ] Before training, map all tokens with frequency below a **threshold of your choice** to `<UNK>` (index 0). 
- [ ] Report and justify this threshold.
- [ ] Handle `<UNK>` (token ID 0) gracefully in all models (validation/test may contain it).

#### Evaluation on validation / test
- [ ] Evaluate each model on **matching categories** from `validation.indexed.csv` (e.g., Type_1 model → Type_1 validation documents).
- [ ] Experiment with **at least three values of k** for Add‑k smoothing. Report results for the best‑performing k.
  - **Requirement for full marks:** Best Add‑k must achieve **≥55% perplexity reduction over Add‑1** in at least 5 categories.
- [ ] Experiment with **at least three values of the discount parameter d** for KN smoothing. Report results for the best‑performing d.
  - **Requirement for full marks:** Best KN must achieve **≥18% perplexity reduction over best Add‑k** in at least 5 categories.
- [ ] After tuning, evaluate all four models on `test_ngram.indexed.csv`.
- [ ] **Console output:** Print a perplexity comparison table with columns:  
  `category`, `MLE`, `Add-1`, `Add-k(k=?)`, `KN(d=?)`  
  Include one row per category plus a **mean row**.
- [ ] **CSV output:** Write the same table to `perplexity_comparison_studentid.csv` with **exact** column names:  
  `category, MLE, Add-1, Add-k(k=<value>), KN(d=<value>)`

#### Notebook requirements for A1
- [ ] All code, experiments, and analysis in a single notebook: `NLP_Assignment1_studentid.ipynb`.
- [ ] Include a Markdown cell at the top with your name, student ID, and a table of contents.
- [ ] Each model must begin with a clearly labelled Markdown heading.
- [ ] Observations must be in Markdown cells after the relevant output.
- [ ] All cells must be executed and outputs visible.

---

### A2 – Catchphrase Insertion (20 marks)

#### Catchphrase selection (2 marks)
- [ ] Choose **one category** (e.g., Type_1, Type_2, …) from the dataset.
- [ ] Select a **catchphrase of three or more tokens** that:
  - Contains **no stopwords** (based on your DU2 stopword list).
  - Is **statistically rare** in the training corpus (e.g., unigram frequency, bigram conditional probability < thresholds, high IDF, etc.).
- [ ] Justify rarity with statistical evidence (token counts, probabilities, etc.) in your notebook.

#### Variation 1 – Data‑level insertion (6 marks)
- [ ] Modify the **training data** (e.g., inject the catchphrase into training documents) to increase the catchphrase’s observed n‑gram counts.
- [ ] In **no more than two sentences**, explain how you modified the data and why this raises catchphrase probability.
- [ ] **Requirement:** Catchphrase appears **at least once in every 60 generated tokens**.

#### Variation 2 – Model‑level insertion (6 marks)
- [ ] Leave training data unchanged. Modify the **model or sampling procedure** (e.g., bias next‑token distribution, forced insertion at fixed intervals).
- [ ] In **no more than two sentences**, explain what you changed and how it encourages catchphrase appearance.
- [ ] **Requirement:** Catchphrase appears **at least once in every 60 generated tokens**.

#### Video submission (6 marks)
- [ ] Record a **screen‑capture video** (max 5 minutes) while generating text live using your model.
- [ ] At the start, give **one sentence** explaining which variation you are demonstrating and how it works.
- [ ] Your **face must be visible** throughout the video.
- [ ] Use your **own live voice** – no AI enhancement, no voiceover tools.
- [ ] Do **not** fast forward.
- [ ] Submit as `studentid_video.mp4` or `.wav`.

---

## Part B – Text Classification – 20 marks total

### B1 – Model Implementation (10 marks)
- [ ] Implement **two classifiers** from scratch (you may use libraries for training, e.g., `sklearn`, but not for the core algorithm logic unless allowed):
  - **Naive Bayes**
  - **Logistic Regression**
- [ ] Train exclusively on corresponding documents from `train.indexed.csv`. The test set `test_classification.indexed.csv` has 5 categories: `Type_1, Type_2, Type_3, Type_4, Type_6`.

#### Pre‑processing & feature engineering
- [ ] Describe any additional pre‑processing steps applied to indexed token sequences before feature extraction. Justify choices.
- [ ] State which features you used (e.g., unigram counts, bigram features, document length). Discuss which features improved/did not improve performance, with experimental evidence.

#### Evaluation on validation split
- [ ] Report **accuracy, macro F1, micro F1** for both models on a validation split held out from `train.indexed.csv`.
- [ ] Present **per‑category F1 scores**.
- [ ] Include a **confusion matrix** for each model.
- [ ] Discuss which model performs better and why.

### B2 – Predictions (10 marks)

- [ ] Apply your **best‑performing model** to `test_classification.indexed.csv`.
- [ ] Fill in the **Category column** with predicted labels.
- [ ] Do **not** alter row order, column order, or any other field.
- [ ] Save the file as `test_classification_indexed_studentid.csv`.

#### Scoring for B2 (based on macro F1 on ground truth)
- Macro F1 < 0.50 → 5 marks  
- Macro F1 ≥ 0.50 → 6 marks  
- Macro F1 ≥ 0.70 → 7–8 marks  
- Macro F1 ≥ 0.80 → 9–10 marks

## Coding Architecture That Should Be Used

- [ ] the viterbi dynamic programming model were used appropriately
- [ ] the forward algorithm was implemented correctly
- [ ] the markov property and hidden markov models were implemented
- [ ] avoiding o(n^3) time complexity
---

## Final Submission – Overall Requirements

### Files to submit (in one ZIP file)
1. `NLP_Assignment1_studentid.ipynb` – master notebook with all sections.
2. `Data_understanding_studentid.csv` – outputs for DU1, DU2, DU3.
3. `perplexity_comparison_studentid.csv` – A1 perplexity table.
4. `studentid_video.mp4` or `.wav` – A2 screen‑capture.
5. `test_classification_indexed_studentid.csv` – B2 predictions.
6. (If applicable) `AI_declaration_studentid.pdf` + chat history – required if you used any generative AI tools.

### Naming conventions
- Replace `studentid` with your **actual student ID** in every filename.
- Column names in CSVs must be **exact** as specified.

### Notebook execution & content
- All cells must be **executed** and outputs visible.
- Observations, justifications, and analysis must be in **Markdown cells**, not code comments.
- Clearly labelled Markdown headings matching the assignment specification.

### Video requirements
- Max 5 minutes.
- Face visible throughout.
- Live voice only – no AI voice, no voiceover tools.
- No fast forwarding.

### Academic integrity
- If you use any method/technology not taught in class, you must **provide citations** and justify why you used them.
- If you used generative AI, submit the AI declaration and full chat history. Failure to do so may result in cross‑examination.

---

## Summary of deliverable files

| File                                          | Section       |
| --------------------------------------------- | ------------- |
| `NLP_Assignment1_studentid.ipynb`             | All           |
| `Data_understanding_studentid.csv`            | DU1, DU2, DU3 |
| `perplexity_comparison_studentid.csv`         | A1            |
| `studentid_video.mp4` or `.wav`               | A2            |
| `test_classification_indexed_studentid.csv`   | B2            |
| `AI_declaration_studentid.pdf` + chat history | If AI used    |
```
dataviewjs // ============ CONFIG ============ const habitEmojis = ["🚶", "🍽️", "😴", "💻", "🪥"]; function isHabit(task) { return habitEmojis.some(emoji => task.text.includes(emoji)); }
```